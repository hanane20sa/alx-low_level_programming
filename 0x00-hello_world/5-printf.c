#include <stdio.h>
/**
 *main - A programm that print a line using printf function
 *Return: 0 (success)
 */
int main(void)
{ ptintf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
